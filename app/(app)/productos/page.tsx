import { ModulePlaceholder } from "@/components/module-placeholder";

export default function Page() {
  return (
    <ModulePlaceholder
      title="Productos"
      description="Catálogo con variantes (talle/color), códigos de barra y etiquetas."
    />
  );
}
